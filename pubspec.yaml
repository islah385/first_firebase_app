name: first_firebase_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.1
  fluttertoast: ^8.2.8
  image_picker: ^1.1.2
  cloud_firestore: ^5.4.1
  firebase_storage: ^12.3.0
  http: ^1.2.2
 
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
